name: Code Build & Deploy
on: [push]
jobs:
  Code-Build-Job:
    runs-on:
      - codebuild-bite-lambda-${{ github.run_id }}-${{ github.run_attempt }}
    steps:
      - uses: actions/checkout@v4
      - name: Build Start
        run: echo "Build Starts..."
